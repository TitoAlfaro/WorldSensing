<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".MainWorldSensing" >

    <View
        android:id="@+id/ColorIndicator"
        android:layout_width="fill_parent"
        android:layout_height="0dp"
        android:layout_gravity="center" >
    </View>
    
    <ToggleButton
        android:id="@+id/LEDBtn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:text="@string/LEDBtnText"
        android:textOff="@string/LEDBtnText"
        android:textOn="@string/LEDBtnText" />

    <TextView
        android:id="@+id/SonarTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/SonarBar"
        android:layout_below="@+id/LEDBtn"
        android:layout_marginTop="23dp"
        android:text="@string/SonarTitle" />

    <SeekBar
        android:id="@+id/SonarBar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_below="@+id/LEDBtn"
        android:layout_marginTop="41dp"
        android:clickable="false"
        android:indeterminate="false"
        android:longClickable="false"
        android:max="500"
        android:progress="0" />

</RelativeLayout>